---
title: Go 的垃圾回收机制
description: 从三色标记、写屏障和并发标记清扫出发，解释 Go GC 的完整周期、触发条件、成本模型与调优方法。
date: 2026-09-03
order: 5
tags:
  - Go
  - Garbage Collection
  - Runtime
  - Memory
---

## 省流

> Go 标准运行时采用精确的、并发的、非分代、非压缩式标记清扫 GC。它从全局变量、各个 Goroutine 栈和运行时对象等根节点出发，沿指针找到所有可达对象；可达对象保留，不可达对象在清扫阶段变成可重新分配的空间。
>
> 标记过程使用三色抽象：白色表示还没有发现，灰色表示已经发现但子引用还没扫描，黑色表示自身和子引用都扫描完成。业务 Goroutine 在并发标记期间还会修改指针，所以运行时启用写屏障，防止一个白色对象被藏到已经扫描过的黑色对象后面而被误回收。Go 还会让分配速度过快的 Goroutine 承担一部分标记工作，也就是 Mutator Assist，避免 GC 永远追不上分配速度。
>
> Go GC 不是完全没有 STW。标记开始和标记终止都需要短暂 STW 来切换状态和完成收尾，大部分扫描工作才是与业务并发执行。清扫也主要并发或在分配时按需进行。
>
> 调优时我不会先手动调用 `runtime.GC()`，而是先看 heap profile、分配速率、存活堆和暂停时间。通常最有效的是减少不必要的堆逃逸、临时对象和指针数量。`GOGC` 控制 CPU 与堆空间的权衡，`GOMEMLIMIT` 提供软内存上限，但设置得低于程序实际需要会造成 GC 抖动。

## 哪些内存由 GC 管？

Go 编译器会做逃逸分析。生命周期能在编译期确定的值通常放在 Goroutine 栈上，函数返回或栈收缩后即可复用，不需要 GC 逐个回收；无法证明只在当前栈帧存活的对象会逃逸到堆上，由 GC 管理。

```text
局部值
  ├─ 生命周期可确定 -> 栈分配
  └─ 逃逸出当前作用域 -> 堆分配 -> GC 跟踪
```

常见逃逸原因包括返回局部变量地址、被闭包捕获、存入生命周期更长的对象，以及某些大小在编译期无法确定的分配。逃逸规则是编译器实现细节，应该通过 `go build -gcflags=-m` 和 profile 验证，而不是死背某条规则。

GC 只负责 Go 堆内存。文件描述符、连接、锁等非内存资源仍应显式 `Close`；不能依赖 Finalizer 在确定时间释放它们。

## 三色标记解决什么问题？

三色不是对象真实的业务状态，而是解释追踪式 GC 的模型：

| 颜色 | 含义 |
| --- | --- |
| 白色 | 本轮尚未证明可达，标记结束后仍为白色就可以回收 |
| 灰色 | 已经可达，但它指向的对象还没有扫描完 |
| 黑色 | 已经可达，而且它指向的对象也扫描完成 |

初始时对象都视为白色，根节点进入灰色集合。GC 反复取出灰色对象，扫描它的指针，把发现的白色对象染灰，再把当前对象染黑。灰色集合清空后，剩余白色对象就是不可达对象。

```text
Roots -> 灰色队列 -> 扫描子引用 -> 黑色
                         |
                         v
                    新发现的灰色对象
```

如果业务线程完全停止，这个算法很直接；难点在于 Go 需要一边标记，一边继续运行和修改对象图。

## 为什么需要写屏障？

并发标记必须维持一个关键不变量：不能让仍存活的白色对象只被黑色对象引用，却再也没有灰色路径能找到它。

例如 GC 已经把对象 A 扫成黑色，此时业务代码把尚未扫描的对象 B 挂到 A 上，同时删除了原来通往 B 的引用。如果没有屏障，GC 不会重新扫描 A，B 可能一直保持白色并被错误回收。

写屏障会在指针写入时执行额外的着色动作，保证这种引用变化仍能被 GC 看见。当前 Go 的混合写屏障可以把需要保护的旧引用或新引用标记起来，并配合“扫描后的 Goroutine 栈视为黑色”，减少反复扫描整栈的成本。

写屏障有 CPU 成本，所以通常只在并发标记阶段开启；栈上的部分写入也会采用不同处理方式。这正是“低暂停”并不等于“GC 免费”：一部分成本从长时间 STW 转移到了并发扫描、写屏障和分配辅助。

## 一次 GC 周期

可以把一次周期概括为四步：

1. **Mark Setup**：短暂 STW，开启写屏障和标记工作。
2. **Concurrent Mark**：恢复业务 Goroutine，GC Worker 并行扫描对象图；分配过快的 Goroutine 执行 Mutator Assist。
3. **Mark Termination**：再次短暂 STW，完成标记收尾并关闭写屏障。
4. **Sweep**：并发或在分配路径上按需清扫未标记对象，把空闲槽位和 Span 交还给分配器复用。

```text
STW           并发运行                         STW          并发/按需
Mark Setup -> Concurrent Mark + Assist -> Mark Termination -> Sweep
```

Go 1.26 起默认启用 Green Tea GC。它优化了小对象标记扫描的内存局部性和多核扩展性，但核心语义仍是并发、非移动的标记清扫，面试时不应把它说成分代或整理式 GC。

## GC 什么时候触发？

最常见的触发来自堆增长目标。`GOGC=100` 的直觉是：以上一轮 GC 后的存活堆和根集合为基准，允许大致再增长 100% 的扫描工作量后启动下一轮。值越小，GC 越频繁、内存峰值通常越低；值越大，GC CPU 压力通常越低、堆占用越高。

`GOMEMLIMIT` 是运行时尽量遵守的软内存限制，适合根据容器限额给运行时留出安全边界。它不是硬隔离：非 Go 内存、内核页缓存和运行时无法立刻回收的存活对象仍可能让进程超过该值。

此外，运行时会避免堆长期不回收，程序也能显式调用 `runtime.GC()`。显式 GC 通常只适合明确的阶段性任务或测试，不是常规性能优化手段。

## GC 成本由什么决定？

核心不是“堆越大一定越慢”，而是以下几项：

- **存活对象和指针数量**：GC 主要扫描可达对象图，含指针多的对象扫描成本更高。
- **分配速率**：每秒制造越多堆对象，越快触发下一轮 GC。
- **根集合大小**：大量 Goroutine 栈和全局指针也需要扫描。
- **目标内存空间**：越严格的内存目标意味着更频繁、更积极的回收。

所以一个大而稳定、指针较少的存活堆，未必比疯狂创建短命指针对象的程序更耗 GC CPU。

## 如何排查和优化？

1. 用 `go tool pprof` 看 `alloc_space` 找累计分配热点，看 `inuse_space` 找当前存活对象和疑似泄漏。
2. 结合 `runtime/metrics`、执行追踪和 `GODEBUG=gctrace=1` 观察 GC 频率、堆目标、暂停和 CPU 占比。
3. 优先减少算法层面的分配，例如预估 Slice/Map 容量、复用缓冲区、避免热路径上的字符串拼接和装箱。
4. 减少不必要的指针和超量 Goroutine，降低扫描工作，而不只是减少字节数。
5. 最后才根据延迟目标和容器内存调整 `GOGC`、`GOMEMLIMIT`，并在真实流量下验证。

`sync.Pool` 适合跨请求复用临时对象，但其中元素可在任意 GC 周期被清除，不能当普通缓存或资源池使用。过度池化还会增加存活堆、保留大容量底层数组，反而使内存更差。

## 扩展问题

### Go GC 是不是没有 STW？

不是。它追求的是短暂停，不是零暂停。标记开始、标记结束以及某些运行时操作仍需要 STW，大部分标记与业务并发执行。

### 为什么 Go 不采用分代 GC？

当前标准运行时选择非分代设计，配合逃逸分析、面向 Span 的分配器和并发标记来控制延迟。分代可以利用“多数对象朝生夕死”的假设，但会引入代际屏障、晋升和更复杂的堆组织。是否分代是实现取舍，不是语言规范保证。

### GC 能解决内存泄漏吗？

只能回收不可达对象。对象如果仍被全局 Map、缓存、Goroutine、Timer 或闭包引用，在 GC 看来就是存活对象，仍然会形成逻辑上的内存泄漏。

## 参考资料

- [A Guide to the Go Garbage Collector](https://go.dev/doc/gc-guide)
- [Go runtime GC implementation](https://go.dev/src/runtime/mgc.go)
- [Go diagnostics](https://go.dev/doc/diagnostics)
- [Go 1.26 Release Notes: Green Tea GC](https://go.dev/doc/go1.26#runtime)
